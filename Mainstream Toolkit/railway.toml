[build]
builder = "DOCKERFILE"
dockerfilePath = "web_platform/Dockerfile"

[deploy]
preDeployCommand = "python manage.py migrate --noinput"
healthcheckPath = "/health/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
